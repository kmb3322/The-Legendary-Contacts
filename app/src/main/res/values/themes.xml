<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Light Theme -->
    <style name="Theme.TheLegendaryContacts" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary, Secondary, Background, Surface 등 참조 -->
        <item name="colorPrimary">@color/md_theme_light_primary</item>
        <item name="colorOnPrimary">@color/md_theme_light_onPrimary</item>
        <item name="android:statusBarColor">@color/md_theme_light_primary</item>
        <!-- 기타 속성들... -->
    </style>

    <!-- Dark Theme -->
    <style name="Theme.TheLegendaryContacts.Dark" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/md_theme_dark_primary</item>
        <item name="colorOnPrimary">@color/md_theme_dark_onPrimary</item>
        <item name="android:statusBarColor">@color/md_theme_dark_primary</item>
        <!-- 기타 속성들... -->
    </style>
</resources>
